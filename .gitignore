*.env
.env.local
.env.*.local

# Raw / large data — never commit (these CSVs are 200MB+, over GitHub's limit)
data/**/*.csv
!data/.gitkeep

# Teammate-provided model report (~23MB) — consumed once by dashboard_data_prep.py
# and pushed into Neon per hotel; the file itself doesn't need to live in git.
data/website_hotel_report.json

# Large generated artifacts (regenerate locally; keep the small smoke-test sample)
outputs/reviews_enriched.parquet
outputs/*.csv

# Python
__pycache__/
*.pyc
*.pyo
*.log

# OS / editor
.DS_Store
Thumbs.db
